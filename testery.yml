test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
                - cypress/e2e/features/google.feature
          then:
            - exclude_tags:
                - web
                - duplicate
