test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
                - cypress/e2e/features/google.feature
          then:
            - exclude_tags:
                - duplicate
            - include_tags:
                - web
