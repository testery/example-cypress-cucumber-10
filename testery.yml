test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
              - cypress/e2e/features/google.feature
          then:
            - include_tags:
              - web
            - test_filters:
              - cypress/e2e/features/google.feature
            - excluded_tags:
              - duplicate
