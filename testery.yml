test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
                - cypress/e2e/features/google.feature
          then:
            - include_tags:
            - exclude_tags:
            - filter_tests:
